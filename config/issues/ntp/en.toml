["public NTP server"]
description = "public NTP server"

["protocol version"]
description = "used version: `{used_version}`"
recommendations = [
  "update to version `{recommended_version}`"
]

["mode 6"]
description = "answers to mode 6 commands (NTPv{version}, opcode {opcode})"
recommendations = [
  "deactivate mode 6"
]

["mode 6 + amplification"]
description = "answers to mode 6 commands (NTPv{version}, opcode {opcode}); amplification factor: {amplification_factor}"
recommendations = [
  "deactivate mode 6"
]

["mode 7"]
description = "answers to mode 7 commands (NTPv{version}, implementation {implementation}, req code {req_code})"
references = [
  "[CVE-2013-5211](https://nvd.nist.gov/vuln/detail/CVE-2013-5211)"
]
recommendations = [
  "deactivate mode 7"
]

["mode 7 + amplification"]
description = "answers to mode 7 commands (NTPv{version}, implementation {implementation}, req code {req_code}); amplification factor: {amplification_factor}"
references = [
  "[CVE-2013-5211](https://nvd.nist.gov/vuln/detail/CVE-2013-5211)"
]
recommendations = [
  "deactivate mode 7"
]

["additional info"]
description = "additional information: {info}"
